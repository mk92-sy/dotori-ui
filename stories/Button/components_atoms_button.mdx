import { Meta } from "@storybook/addon-docs/blocks";
import * as ButtonStories from "./components_atoms_button.stories";

<Meta of={ButtonStories} />

# Button

버튼은 한번의 클릭을 통해 사용자가 행동하고 선택할 수 있도록 하는 인터페이스입니다.

## Structure

```tsx
import { Button } from "dotori-ui";

<Button>Button Text</Button>;
```
